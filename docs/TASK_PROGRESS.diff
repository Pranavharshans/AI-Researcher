diff --git a/product-plan/tasks b/product-plan/tasks
new file mode 100644
--- /dev/null
+++ b/product-plan/tasks
@@
+# Agentic LaTeX Diagram Editor: Task Progress Tracker
+#
+# Status legend:
+# [x] completed
+# [~] in progress
+# [ ] not started
+# [!] blocked or needs decision
+#
+# This file tracks product and implementation progress in a diff-style ledger.
+
+[x] TASK-001 Install bencium marketplace skills
+    Result: Installed 14 bencium skills with `npx skills add bencium/bencium-marketplace -g --all`.
+    Installed location reported by CLI: ~/.agents/skills/*
+    Note: Restart Codex to auto-discover newly installed global skills.
+
+[x] TASK-002 Install UI UX Pro Max for Codex
+    Result: Installed `uipro-cli` globally with npm.
+    Result: Installed UI UX Pro Max project-local with `uipro init --ai codex`.
+    Installed location: .codex/skills/ui-ux-pro-max
+    Note: The current `uipro` version rejected the documented `--global` flag, so project-local install was used.
+
+[x] TASK-003 Apply requested skills to planning
+    Applied: bencium-controlled-ux-designer for production UX, accessibility, context menus, dialogs, and approval flow.
+    Applied: UI UX Pro Max for developer-tool design-system guidance, z-index/accessibility rules, and typography guidance.
+    Applied: human-architect-mindset from bencium for AI-aware decomposition and verifiable agent boundaries.
+
+[x] TASK-004 Create detailed PRD
+    Output: docs/PRD_AGENTIC_LATEX_DIAGRAM_EDITOR.md
+    Includes: UX, architecture, state machine, self-healing repair loop, model abstraction, OpenRouter role, APIs, data model, tests, risks.
+
+[x] TASK-005 Create progress tracker
+    Output: docs/TASK_PROGRESS.diff
+
+[x] TASK-006 Create test plan/results ledger
+    Output: docs/TEST_RESULTS.md
+
+[x] TASK-007 Scaffold application shell
+    Target: Next.js + TypeScript app with Overleaf-like layout.
+    Acceptance: file tree, Monaco editor, preview panel, compile status bar.
+    Result: Created Next.js App Router shell with file tree, Monaco-backed LaTeX editor, PDF preview panel, AI status rail, compile button, and bottom status bar.
+    Verification: `npm test`, `npm run typecheck`, `npm run lint`, `npm run build`, and Playwright desktop/mobile smoke checks passed.
+
+[x] TASK-008 Implement editor right-click menu
+    Acceptance: context menu opens on editor right-click and includes "Add diagram".
+    Acceptance: keyboard accessible, Escape closes, no layout overlap.
+    Result: Added editor-scoped custom context menu with Add diagram, disabled future actions, Escape/outside close behavior, arrow-key navigation, and toolbar fallback for touch/keyboard users.
+    Verification: `npm test`, `npm run typecheck`, `npm run lint`, `npm run build`, and Playwright right-click/toolbar smoke checks passed.
+
+[x] TASK-009 Implement Add Diagram dialog
+    Acceptance: prompt entry, style preset, generate/cancel buttons, validation.
+    Result: Added accessible Add Diagram modal with required prompt textarea, 4,000 character guardrail, style preset selection, output target selection, Cancel, and Generate Diagram.
+    Verification: `npm test`, `npm run typecheck`, `npm run lint`, `npm run build`, and Playwright dialog validation/submission smoke checks passed.
+
+[x] TASK-010 Implement model provider abstraction
+    Acceptance: internal ModelClient interface and OpenRouterProvider behind it.
+    Result: Added provider-neutral `ModelClient`, model request/response/event types, explicit model task schemas, OpenRouter request normalization, OpenRouter provider adapter, environment config, and `.env.example`.
+    Verification: `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build` passed. Live OpenRouter calls skipped because no API key is provided.
+
+[x] TASK-011 Implement LaTeX compiler sandbox
+    Acceptance: standalone figure compilation with timeout, no shell escape, artifact capture.
+    Result: Added server-side LaTeX compiler sandbox boundary with standalone TikZ wrapper, per-job temporary workspace, Docker TeX Live command builder, network-disabled Docker args, CPU/memory limits, no-shell-escape latexmk flags, timeout-enabled process runner, compile log capture, and artifact inventory.
+    Verification: `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build` passed. Docker/TeX execution was not run in this environment.
+
+[x] TASK-012 Implement LaTeX log parser
+    Acceptance: extracts errors, file, line, normalized type, source context, and hints.
+    Result: Added server-side LaTeX log parser with structured error objects, file/line extraction, normalized error types, source context windows, TikZ missing-library hints, and unknown-node mismatch hints.
+    Verification: `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build` passed. Fixture tests cover basic LaTeX errors, missing TikZ positioning library, and unknown TikZ node detection.
+
+[x] TASK-013 Implement agentic repair state machine
+    Acceptance: observe -> parse -> diagnose -> plan -> patch -> verify loop.
+    Important: max attempts are a safety budget only, not the repair algorithm.
+    Result: Added server-side repair state machine with explicit workflow phases, compile observation handling, evidence-carrying attempts, diagnosis and patch-plan records, recompile verification transitions, repeated-error escalation, failure explanation, acceptance, insertion, main compile, done, and user revision transitions.
+    Verification: `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build` passed. State-machine fixture tests cover failed compile -> diagnosis -> patch planning -> patch application -> recompile -> preview, repeated-error escalation, and user revision branch.
+
+[x] TASK-014 Implement structured patch applier
+    Acceptance: applies model-proposed edits safely, supports rollback, restricts paths.
+    Result: Added structured patch applier with workspace path validation, supported edit operations, before/after records, rollback support, and security events for rejected paths or operations.
+    Verification: `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build` passed. Fixture tests cover valid replace, rollback, path traversal rejection, absolute path rejection, and append.
+
+[x] TASK-015 Implement diagram preview and approval
+    Acceptance: preview ready state asks "Does this match what you wanted?"
+    Acceptance: Keep Diagram, Request Changes, Discard.
+    Result: Added typed preview approval state, compiled diagram preview surface, accessible summary, repair/change summary, collapsible source preview, and approval actions for Keep Diagram, Request Changes, and Discard.
+    Verification: `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build` passed. Fixture tests cover preview-ready state creation, keyless OpenRouter behavior, approval action wiring, accessible labels/live regions, source preview, and responsive approval styling.
+
+[x] TASK-016 Implement user revision loop
+    Acceptance: user feedback modifies existing diagram source and reruns compile/repair.
+    Result: Added Request Changes feedback form, revision validation, existing-source revision history, deterministic local source patching for keyless operation, recompile status, and return to preview approval after revision.
+    Verification: `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build` passed. Fixture tests cover feedback prompt, validation, revision submission, existing-source reuse, targeted source changes, revision history, recompile status, and responsive revision form styling.
+
+[ ] TASK-017 Implement insertion into main document
+    Acceptance: approved diagram is inserted at target cursor/selection.
+    Acceptance: generated figure source is saved under figures/generated.
+
+[ ] TASK-018 Implement test suite
+    Acceptance: unit, integration, e2e, visual, and security tests from docs/TEST_RESULTS.md.
+
